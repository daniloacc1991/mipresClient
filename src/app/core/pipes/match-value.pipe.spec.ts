import { MatchValuePipe } from './match-value.pipe';

describe('MatchValuePipe', () => {
  it('create an instance', () => {
    const pipe = new MatchValuePipe();
    expect(pipe).toBeTruthy();
  });
});
