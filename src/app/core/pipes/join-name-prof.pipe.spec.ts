import { JoinNameProfPipe } from './join-name-prof.pipe';

describe('JoinNameProfPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinNameProfPipe();
    expect(pipe).toBeTruthy();
  });
});
